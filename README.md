1. Después de clonar el proyecto ejecutar dentro de las carpetas backend y frontend:
    $ npm install

2. Una vez que termine de instalar los paquetes ejecutar:
    $ docker compose up --build -d

3. Se debió haber generado 3 contenedore:
    1. db
    2. backend
    3. frontend